for number in num:
    while ST and ST[-1] == now:  # 스택의 맨 위가 현재 필요한 수와 일치하면 제거
        ST.pop()
        now += 1
    if number == now:  # 현재 순회 중인 숫자가 필요한 수와 일치하면 진행
        now += 1
    else:  # 일치하지 않으면 스택에 추가
        ST.append(number)

이 문제는 순서가 없는 수열에서 순서대로 숫자 출력이 가능한지를 확인하는 문제이다.
현재 필요한 숫자와 현재 순회 중인 숫자가 일치하면 진행, 아니면 스택에 추가하는
위와 같은 코드를 작성할 수 있음.